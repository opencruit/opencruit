apiVersion: 1

providers:
  - name: OpenCruit
    orgId: 1
    folder: OpenCruit
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
